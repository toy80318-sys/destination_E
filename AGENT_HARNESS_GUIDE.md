# 데스티네이션 어스 — 서브에이전트·루프·하네스 지침서

> 이 문서는 본 프로젝트의 멀티 에이전트 개발 체계의 **단일 기준(source of truth)** 이다.
> CLAUDE.md가 "무엇을 지켜야 하는가"라면, 이 문서는 "누가·어떤 순서로·어떻게 실행하는가"를 정의한다.
> 관련 파일: `.claude/agents/*.md`(에이전트 정의) · `.claude/commands/dev-loop.md`(루프 명령) · `DEV_BACKLOG.md`(작업 큐) · `DEV_LOOP_LOG.md`(실행 기록)

---

## 0. 왜 이 구조인가 — 프로젝트 특성과 설계 근거

| 프로젝트 특성 | 하네스 설계 결정 |
|---|---|
| 테스트 프레임워크 없음 (바닐라 JS) | 검증 = `node --check` + **Node 격리 시뮬레이션**(브라우저 전역 목킹)으로 대체. Tester가 매 사이클 시뮬레이션 스크립트를 작성·실행 |
| GUI 검증 불가 (에이전트는 Electron 창을 못 봄) | DOM 렌더 결과는 검증 범위에서 제외하고 **로직·상태·데이터 흐름**만 검증. UI 확인이 필수인 항목은 `❓ 결정 대기`로 사람에게 넘김 |
| `game.js` 대형 단일 파일 + 모듈 39종 | Coder에게 "모듈 분리 우선" 규칙 적용. 리뷰에서 game.js 비대화를 결함으로 지적 |
| 출시 후 세이브 호환성 절대 보호 | 세이브 포맷에 닿는 변경은 **마이그레이션 코드 없으면 하네스가 커밋을 차단** |
| 밸런싱·디자인 결정은 사람 소관 | 수치 변경이 필요한 작업은 자동 처리 금지 → `❓` 마킹 후 건너뜀 |
| i18n ko/en 이중 언어 | 사용자 노출 문자열 변경 시 `verify-i18n-live.js` 필수 통과 |
| 원격/헤드리스 세션에서 주로 구동 | 루프는 "사이클당 1건 + 파일 기반 상태(백로그/로그)"로 설계 — 세션이 끊겨도 다음 세션이 파일만 읽고 이어감 |

---

## 1. 역할 구조 (하네스 토폴로지)

```
사람 (최종 결정권자)
 │  결정 대기(❓) 항목 승인 / STOP 플래그 / 백로그 추가
 ▼
Director (메인 세션 — /dev-loop 실행 주체)
 │  작업 선택·위임·판정·커밋·푸시만 한다. 직접 코드를 고치지 않는다.
 ├──▶ de-coder    (구현. 유일한 코드 수정 권한)
 ├──▶ de-tester   (검증. 실행·보고만)  ┐ 병렬 실행
 └──▶ de-reviewer (리뷰. 지적만)      ┘
```

**권한 분리 3원칙** (CLAUDE.md 규칙의 하네스 구현):
1. 코드를 쓰고 고치는 권한은 **de-coder에게만** 있다.
2. de-reviewer·de-tester는 문제를 **지적·보고만** 하고 절대 고치지 않는다.
3. 커밋·푸시·백로그 갱신은 **Director만** 한다. 서브에이전트는 git 상태를 바꾸지 않는다.

### 1.1 에이전트별 계약 (Input / Output Contract)

#### de-coder (구현)
- **입력**: 백로그 항목 원문 + 관련 파일 경로 + 완료 기준(Definition of Done) + (재위임 시) 수정 지적 목록
- **출력**: ① 변경 파일 목록 + 파일:라인 요지 ② 실행한 검증 명령과 결과 ③ 남은 문제("없음" 명시 가능)
- **도구**: 전체 상속. **금지**: git commit/push, 백로그·로그 파일 수정, 범위 밖 파일 수정
- **자체 검증 의무**: 수정한 모든 `.js`에 `node --check`. 텍스트 변경 시 i18n 검증. 실패 상태로 반환 금지

#### de-tester (검증)
- **입력**: 변경 파일 목록(또는 `git diff --name-only HEAD`) + 작업의 의도(무엇이 되어야 하는가)
- **출력**: ① 검증 항목별 통과/실패 표 ② 실패 시 재현 명령 + 에러 원문 + 추정 원인 ③ 최종 판정 **통과/실패**
- **도구**: Read, Grep, Glob, Bash, Write(스크래치패드 전용)
- **표준 절차**: §3 검증 파이프라인을 순서대로 실행. 미실행 항목은 "미실행"으로 명시 (침묵 = 실패로 간주)

#### de-reviewer (리뷰)
- **입력**: `git diff` 범위 + 작업 의도
- **출력**: `[심각도] 파일:라인 — 문제 — 근거 — 권장 방향` 목록, 또는 "결함 없음 — 통과"
- **도구**: Read, Grep, Glob, Bash(읽기 전용 — sed -i, >, mv, rm 금지)
- **리뷰 우선순위**: 정확성 버그 > 세이브 호환성 > 프로젝트 규칙 위반(하드코딩·비밀키·game.js 비대화) > i18n 누락 > 명백한 성능 낭비
- **지적 금지**: `@crumbs` 임시 계측, 취향성 스타일

### 1.2 모델·비용 라우팅

| 상황 | 선택 |
|---|---|
| 기본 | 세 에이전트 모두 `model: inherit` (세션 모델 따름) — 현 설정 |
| 단순·기계적 작업(파일 이동, 일괄 치환, 문서 생성) | Director가 위임 시 effort를 낮추거나 haiku 지정 가능 |
| 동시 실행 | **최대 3개** (tester+reviewer 병렬이 표준). 그 이상은 병합 비용이 이득을 상쇄 |
| 대형 탐색(코드 위치 찾기) | 커스텀 에이전트 대신 내장 **Explore** 사용 — 요약만 회수해 컨텍스트 절약 |

---

## 2. 루프 프로토콜 (/dev-loop 상태 머신)

한 사이클 = 백로그 **정확히 1건**. 작게, 자주, 복구 가능하게.

```
[0 안전점검] ─실패→ 중단·보고
     │
[1 작업선택] ─백로그 소진→ 루프 종료(크론 삭제)
     │            ─❓/⏸ 항목→ 건너뛰고 다음 항목
[2 구현: de-coder]
     │
[3 검증·리뷰: tester ∥ reviewer]
     │
     ├─ 전부 통과 ──────────────▶ [5 마무리: 체크·로그·커밋·푸시]
     │
     └─ 실패/지적 → [4 수정: de-coder] → 3으로 (최대 2회)
                        │
                        └─ 2회 초과 → git checkout -- . (원복)
                                      백로그에 ⚠ 표시 → 다음 사이클
```

### 2.1 단계별 세부 규칙

**[0] 안전 점검** — 하나라도 걸리면 사이클을 시작하지 않는다:
- `git status` 불결(이전 잔여물) → 중단·보고. *예외: DEV_LOOP_LOG.md만 더러운 경우는 직전 사이클의 로그 미커밋이므로 먼저 커밋하고 진행*
- `DEV_BACKLOG.md` 중단 플래그 섹션에 `STOP` → 중단
- `DEV_LOOP_LOG.md` 최근 2개 항목이 연속 실패 → 중단하고 사람 확인 요청 (동일 원인 무한 반복 방지)

**[1] 작업 선택**:
- 위에서부터 첫 `- [ ]` 항목. `$ARGUMENTS`로 번호 지정 시 해당 항목
- 항목이 "한 사이클에 못 끝낼 크기"로 판단되면: 구현하지 말고 백로그에서 하위 항목으로 분해한 뒤 **첫 조각만** 이번 사이클에서 처리
- 크기 판단 기준: 예상 변경 파일 4개 초과, 또는 서로 다른 시스템 2개 이상에 걸침 → 분해 대상

**[2] 구현 위임** — 위임 프롬프트에 반드시 포함할 것:
```
작업: <백로그 항목 원문>
관련 파일: <경로들>
완료 기준: <검증 가능한 조건. 예: "X 함수가 입력 Y에 Z를 반환. node --check 통과">
금지: 범위 밖 파일 수정, 밸런싱 수치 변경, 커밋
```

**[3] 판정 기준** — Director가 종합 판정:
- tester **실패** = 무조건 재수정 대상
- reviewer **높음** = 재수정 대상 / **중간** = 이번 사이클 내 수정 권장 / **낮음·추정** = 백로그에 후속 항목으로 적고 통과 허용

**[4] 수정 반복 한도 = 2회**: 3번째 실패는 접근 자체가 잘못됐다는 신호다. 커밋하지 않고 원복 후 `⚠ 자동처리 실패(사유 1줄)` 마킹. 사람이 백로그에서 사유를 보고 재지시한다.

**[5] 마무리 커밋 규칙**:
- 커밋 메시지: `<type>(<scope>): <요지>` — 기존 히스토리 관례(feat/fix/chore + 한국어 요지) 준수
- 백로그 체크(`- [x] ... (완료: YYYY-MM-DD)`)와 로그 추가를 **같은 커밋에 포함** (상태와 코드가 항상 원자적으로 동기화)
- 푸시 실패 시 지수 백오프 재시도(2s/4s/8s/16s)

### 2.2 루프 구동 방법

| 환경 | 방법 | 권장 주기 |
|---|---|---|
| 대화형(로컬 CLI/데스크톱) | `/loop 30m /dev-loop` | 30분 — 사이클(구현+검증)이 보통 10~20분이므로 겹침 방지 |
| 원격/헤드리스(웹 세션) | Director가 크론 등록 (예: `13 * * * *`) | 1시간 — 사람이 로그를 따라올 수 있는 속도 |
| 수동 | `/dev-loop` 단발 실행 | 필요할 때 1건씩 |

**중단 방법 (우선순위 순)**: ① `DEV_BACKLOG.md`에 `STOP` 기입(다음 사이클부터 정지, 가장 안전) ② 크론/루프 삭제 ③ 채팅으로 "루프 중단" 지시

### 2.3 백로그 문법 (`DEV_BACKLOG.md`)

```
- [ ] N. <작업 제목> — <완료 기준 요지> (<관련 파일>)   ← 대기
- [x] N. ... (완료: 2026-07-02)                        ← 완료
- ⏸ N. ... — <보류 사유>                               ← 보류(루프 건너뜀)
- ❓ N. ... — <필요한 결정>                             ← 결정 대기(루프 건너뜀)
- ⚠ N. ... — 자동처리 실패(<사유>)                      ← 실패(사람 재지시 대기)
```
- 새 작업 추가는 사람 또는 Director가 한다(서브에이전트 금지). 항목은 반드시 **검증 가능한 완료 기준**을 갖는다.
- 우선순위 = 문서상 순서. 재정렬은 사람이 한다.

---

## 3. 검증 파이프라인 (하네스의 품질 게이트)

모든 사이클은 아래 게이트를 순서대로 통과해야 커밋된다. 앞 게이트 실패 시 뒤는 실행하지 않는다.

| # | 게이트 | 명령/방법 | 적용 조건 |
|---|---|---|---|
| G1 | 구문 | `node --check <파일>` | 변경된 모든 `.js` — 무조건 |
| G2 | i18n | `node scripts/verify-i18n-live.js` | 사용자 노출 문자열·번역 변경 시 |
| G3 | 기능 시뮬레이션 | 스크래치패드에 Node 스크립트 작성: 브라우저 전역(`localStorage`·`document`·`window`) 목킹 → 변경 함수를 정상 경로 + 엣지 케이스(경계값, null/undefined, 깨진 입력, 대형 입력)로 실행 | 로직 변경 시 — 무조건 |
| G4 | 세이브 호환 | 구버전 세이브 객체(버전 필드 낮음/필드 누락)를 로드 경로에 투입해 크래시·데이터 손실 없는지 확인 | 세이브 포맷·로드 경로에 닿는 변경 시 — **마이그레이션 없으면 무조건 실패 처리** |
| G5 | 회귀 | 핵심 흐름(새 게임 → 행성 순회 → 영웅 영입 → 엔딩)에 닿는 함수를 시뮬레이션 1회 | 진행 흐름·상태 구조 변경 시 |
| G6 | 리뷰 | de-reviewer 판정 (§1.1 우선순위) | 무조건 |

**전체 패키징 빌드(`dist:win` 등)는 루프에서 돌리지 않는다** — RC(출시 후보) 시점 또는 빌드 파이프라인 변경 시에만 사람 지시로 수행한다.

---

## 4. 작업 유형별 라우팅 표

| 작업 유형 | 자동 처리 | 담당 흐름 | 필수 게이트 |
|---|---|---|---|
| 버그 수정 | ✅ | coder → tester∥reviewer | G1·G3 + 해당 시 G4·G5 |
| 신규 기능(모듈) | ✅ (단, `js/modules/` 신규 파일로) | coder → tester∥reviewer | G1·G3·G6 + 해당 시 G2 |
| 모듈 분리 리팩터 | ✅ (로드맵 명시 항목만) | coder → tester∥reviewer, 경계 변수 참조 검증 필수 | G1·G3·G5 |
| QA 검증 항목 | ✅ (코드 변경 없음) | tester 단독 → 결과를 로그·백로그에 기록 | 해당 게이트만 |
| 문서 작성 | ✅ | coder(또는 Director 직접) | 없음 (리뷰 선택) |
| 밸런싱 수치 변경 | ❌ `❓` | 사람 결정 → 결정 후 일반 버그 수정 흐름 | — |
| UI 시각 확인 필수 작업 | ❌ `❓` | 로직까지만 자동, 시각 확인은 사람 | — |
| 세이브 포맷 변경 | ⚠ 조건부 | 마이그레이션 코드 동반 시에만 자동 허용 | G4 무조건 |
| 대규모 리팩터(시스템 2개+) | ❌ `❓` | Director가 영향 범위 요약 → 사람 승인 후 분해 투입 | — |
| 에셋 추가/교체 | ❌ `❓` | 라이선스 확인은 사람. 확인 후 참조 연결만 자동 | 정적 참조 검증 |

---

## 5. 안전장치 총목록 (하네스 불변식)

1. **사이클당 1건** — 폭주 방지의 1차 방어선
2. **STOP 플래그** — 사람이 언제든 파일 한 줄로 정지
3. **연속 2회 실패 시 자동 정지** — 같은 원인 무한 재시도 차단
4. **수정 2회 초과 시 원복** — 실패한 접근을 커밋하지 않음. 작업 트리는 항상 "통과했거나 깨끗하거나"
5. **❓/⏸ 건너뜀** — 사람 소관(디자인·라이선스·시각 확인)을 자동화가 침범하지 않음
6. **세이브 호환 게이트(G4)** — 출시 후 세이브 파손 사고의 구조적 차단
7. **권한 분리** — 검증자가 수정 권한을 갖지 않으므로 "검증을 통과시키기 위한 검증 조작" 불가
8. **파일 기반 상태** — 세션이 죽어도 백로그+로그만으로 재개 가능. 컨텍스트에 상태를 저장하지 않는다
9. **커밋 원자성** — 코드+백로그+로그가 한 커밋. 어느 시점을 봐도 상태 일관
10. **비밀·산출물 커밋 금지** — `dist/`·키 파일은 .gitignore가 차단, 리뷰어가 이중 확인

---

## 6. 사람의 개입 지점 (Human-in-the-loop)

| 시점 | 사람이 하는 일 |
|---|---|
| 백로그 관리 | 항목 추가·우선순위 재정렬·`⏸` 해제 |
| `❓` 항목 | 결정을 내리고 항목을 `- [ ]`로 되돌림 (그러면 루프가 집어감) |
| `⚠` 항목 | 실패 사유를 보고 재지시(분해·접근 변경) 또는 폐기 |
| 연속 실패 정지 | 로그 확인 후 재가동 지시 |
| PR 머지 | draft 해제·머지는 항상 사람이 결정 |
| RC 빌드 | 전체 패키징·Steam 업로드 지시 |

**루프가 사람을 부르는 방식**: 백로그 마킹(❓/⚠) + DEV_LOOP_LOG.md 기록 + (원격 세션이면) 채팅 요약 보고. 루프는 사람 응답을 기다리며 멈추지 않는다 — 마킹하고 다음 항목으로 넘어간다.

---

## 7. 확장 로드맵 (지금은 하지 않는 것)

- **에이전트 추가는 필요가 증명될 때만.** "역할이 있으면 좋겠다"가 아니라 "같은 종류의 위임을 3회 이상 반복했다"가 추가 기준. 후보: `de-i18n`(번역 일괄 작업이 잦아지면), `de-balance-analyst`(읽기 전용 밸런스 리포트 — 수치 변경은 여전히 사람)
- **Workflow(다중 에이전트 오케스트레이션)**: 백로그 항목이 "24행성 전체 X 일괄 검증"처럼 대량 팬아웃이 필요할 때만 사람 승인 하에 사용
- **에이전트 팀(상호 통신)**: 현 규모에서 불필요. 단일 Director 오케스트레이션으로 충분
- **Steamworks 연동·코드 서명**: GUI/계정 작업 — 하네스 범위 밖, 사람 전담

---

## 부록 A. 사이클 위임 프롬프트 템플릿

```
# de-coder 위임
작업: [백로그 항목 원문]
관련 파일: [경로]
완료 기준: [검증 가능한 조건]
금지: 범위 밖 수정, 밸런싱 변경, 커밋
현재 브랜치: [브랜치명] (커밋은 Director가 한다)

# de-tester 위임
변경 파일: [git diff --name-only HEAD 결과]
작업 의도: [한 줄]
실행할 게이트: G1 + [G2~G5 중 해당]
판정: 통과/실패 + 실패 시 재현 명령·에러 원문

# de-reviewer 위임
리뷰 범위: git diff [기준]
작업 의도: [한 줄]
우선순위: 정확성 > 세이브 호환 > 규칙 위반 > i18n > 성능
```

## 부록 B. 시뮬레이션 스크립트 골격 (G3/G4용)

```js
// 스크래치패드에 작성 — 저장소에 커밋하지 않는다
const store = new Map();
global.localStorage = {
  getItem: k => store.has(k) ? store.get(k) : null,
  setItem: (k, v) => store.set(k, String(v)),
  removeItem: k => store.delete(k),
};
global.document = { getElementById: () => null, querySelector: () => null,
  createElement: () => ({ style: {}, classList: { add(){}, remove(){} } }) };
global.window = global;
// 대상 함수를 require/추출해 정상 + 엣지 케이스 실행, 실패 시 process.exit(1)
```
