# DEV_LOOP_LOG — 지속 개발 루프 실행 기록

`/dev-loop`가 사이클마다 맨 위에 결과를 추가한다. (최신이 위)

형식:
```
## YYYY-MM-DD HH:MM — 항목 N: <제목>
- 결과: 통과 / 실패(사유)
- 변경 파일: ...
- 특이사항: ...
```

---

## 2026-07-05 — 항목 M1: 모달 → 바텀시트 전환 (모바일)
- 결과: **통과** (구현 → 검증·리뷰 → 수정 1회차 → 재검증·재리뷰 통과. 수정 한도 2회 중 1회 사용)
- 변경 파일: game.css (1차 +36줄 커밋 5f7b5b9, 수정 1회차 43+/34-)
- 특이사항: ① 리뷰 [높음] — `_isMobileFit()` 높이 단독 판정으로 1366×768 데스크톱 누수 → 바텀시트 블록을 `@media(pointer:coarse)` 이중 게이트로 방어(백로그 15 후속 등록). ② holo 테마 clip-path 노치 충돌 → `clip-path:none`(명시도 승리 검증). ③ 세이프에어리어 env() 가산은 스테이지 `_safeInsets()` 보정과 이중이라 제거(좌표계 근거 tester·reviewer 교차 확인). ④ 닫기 퇴장 모션은 백로그 14로. 실기기 시각 확인(::before 프레임 아티팩트 등)은 M9(사람)에서.
- 결과: **통과** (시뮬레이션 22/23 — de-tester 단독 QA 흐름, 코드 변경 없음)
- 변경 파일: 없음 (검증 스크립트는 스크래치패드에만 작성)
- 특이사항: ① 재시작·프로세스 재생성·구버전 세이브 재생성 모두 바이트 단위 동일 배치 확인 — 진행 파괴 위험 없음. ② `generateGalaxy(seed)`의 seed 파라미터가 데드 코드(본문 미참조, 배치는 PLANET_DEF ring/ang + ID 해시로 결정) — 버그 아님, 후속 정리 항목 13으로 등록. ③ 세이브에는 시드가 아닌 지도 자체(`G.mapPositions`/`G.mapConns`)가 저장되고, 누락 시 재생성도 상수적이라 어느 경로든 동일.
